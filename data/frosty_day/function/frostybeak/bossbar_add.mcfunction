$bossbar add $(bossbar_add) {"translate": "frostybeak"}
$bossbar set $(bossbar_add) color blue
$bossbar set $(bossbar_add) max 230
$bossbar set $(bossbar_add) players @a[distance=..48]
$bossbar set $(bossbar_add) visible true